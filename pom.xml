<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.nuxeo</groupId>
        <artifactId>nuxeo-parent</artifactId>
        <version>2023.33</version>
    </parent>

    <groupId>se.dms.diarium.frontend</groupId>
    <artifactId>dms-frontend-parent</artifactId>
    <version>0.1.0</version>
    <packaging>pom</packaging>
    <name>DMS frontend parent</name>
    <description>DMS Diarium Angular/Nuxeo based parent project</description>

    <properties>
        <!-- Skip Nuxeo enforcer rules by default -->
        <nuxeo.skip.enforcer>true</nuxeo.skip.enforcer>
        <nuxeo.target.version>${nuxeo.platform.version}</nuxeo.target.version>
        <skipDocker>false</skipDocker>
        <pushReleaseChanges>false</pushReleaseChanges>
    </properties>

    <modules>
        <module>dms-frontend-web</module>
        <module>dms-frontend-package</module>
    </modules>

    <scm>
        <connection>scm:git:${project.scm.url}</connection>
        <developerConnection>scm:git:${project.scm.url}</developerConnection>
        <url>https://github.com/nuxeo-sandbox/nuxeo-angular-sample-app-packaging</url>
    </scm>

    <licenses/>
    <mailingLists/>
    <issueManagement/>
    <ciManagement/>
    <developers/>
    <distributionManagement>
        <repository>
            <id>teamnordiq-releases</id>
            <url>https://nexus.teamnordiq.se/repository/releases</url>
        </repository>
        <snapshotRepository>
            <id>teamnordiq-snapshots</id>
            <url>https://nexus.teamnordiq.se/repository/snapshots</url>
        </snapshotRepository>
    </distributionManagement>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>se.dms.diarium.frontend</groupId>
                <artifactId>dms-frontend-web</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>se.dms.diarium.frontend</groupId>
                <artifactId>dms-frontend-package</artifactId>
                <version>${project.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <repositories>
        <repository>
            <id>maven-public</id>
            <url>https://packages.nuxeo.com/repository/maven-public/</url>
            <releases>
                <enabled>true</enabled>
            </releases>
            <snapshots>
                <updatePolicy>always</updatePolicy>
                <enabled>true</enabled>
            </snapshots>
        </repository>
        <repository>
            <id>maven-private</id>
            <url>https://packages.nuxeo.com/repository/maven-private/</url>
            <releases>
                <enabled>true</enabled>
            </releases>
            <snapshots>
                <updatePolicy>always</updatePolicy>
                <enabled>true</enabled>
            </snapshots>
        </repository>
    </repositories>

    <pluginRepositories>
        <pluginRepository>
            <id>maven-public</id>
            <url>https://packages.nuxeo.com/repository/maven-public/</url>
            <releases>
                <enabled>true</enabled>
            </releases>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </pluginRepository>
    </pluginRepositories>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-release-plugin</artifactId>
                <version>3.1.1</version>
                <configuration>
                    <projectVersionPolicyId>SemVerVersionPolicy</projectVersionPolicyId>
                    <tagNameFormat>@{project.version}</tagNameFormat>
                    <autoVersionSubmodules>true</autoVersionSubmodules>
                    <scmReleaseCommitComment>Release @{releaseLabel}</scmReleaseCommitComment>
                    <scmDevelopmentCommitComment>Post Release @{releaseLabel}</scmDevelopmentCommitComment>
                    <pushChanges>${pushReleaseChanges}</pushChanges>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>versions-maven-plugin</artifactId>
                <version>2.18.0</version>
                <configuration>
                    <ruleSet/>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
